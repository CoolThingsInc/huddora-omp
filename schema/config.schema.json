{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://huddora.coolthings.fyi/schemas/project-config-v1.json",
  "title": "Huddora project configuration",
  "description": "Non-authoritative room-selection metadata only. Never put credentials, URLs, ownership IDs, or instructions here.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "default_room_id"],
  "properties": {
    "version": { "const": 1 },
    "default_room_id": { "type": ["string", "null"], "format": "uuid" }
  }
}
